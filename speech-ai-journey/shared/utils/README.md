# Utilitaires partagés

Fonctions réutilisables à travers toutes les phases.

## Fichiers prévus

- `audio.py` — chargement, resampling, padding/trimming
- `metrics.py` — calcul WER, CER, DNSMOS
- `visualization.py` — affichage spectrogramme, courbes d'entraînement
- `data_utils.py` — collate functions, DataLoader helpers

---

# Shared Utilities *(English)*

Reusable functions shared across all phases.

## Planned Files

- `audio.py` — loading, resampling, padding/trimming
- `metrics.py` — WER, CER, DNSMOS computation
- `visualization.py` — spectrogram display, training curves
- `data_utils.py` — collate functions, DataLoader helpers
